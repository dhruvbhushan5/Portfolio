var typed = new Typed('#element', {
    strings: ['Web Developer', 'Gamer', ' Video Editior', 'Web Designer'],
    typeSpeed: 50,
});




// typed is a variable that stores the new instance of the Typed class.
// new Typed('#element', {...}) creates this instance and applies the typing effect to the HTML element with the ID #element.


 // Explanation:
// The Typed instance targets the HTML element with the ID #element.
// The strings array defines the text that will be typed out in sequence.
// The typeSpeed setting controls the speed of the typing effect.
// The typing animation will cycle through each string in the strings array, typing each one character by character at the specified speed.